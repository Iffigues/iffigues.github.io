---
layout: default
title: School 42
---
<div class="page-42-wrapper">
    <header class="section-header">
        <h1><span class="glitch">42</span> : Le cursus</h1>
        <div class="main-nav-buttons">
            <button id="btn-parcours" class="btn-42">⚡ Mon Parcours</button>
            <button id="btn-documents" class="btn-42">📂 Mes Documents</button>
        </div>
    </header>

    <div id="comment" class="content-card">
        <div class="story-layout">
            <div class="image-container">
                <img src="{{ '/assets/data/42/img/bordenoy.jpg' | relative_url }}" alt="Photo 42" class="profile-img">
            </div>
            <div class="text-content">
                <section id="parcours-42" class="parcours">
                    <h2>🎓 Mon Parcours</h2>
                    <p>Mon aventure à 42 a commencé en <strong>septembre 2018</strong> avec la <strong>piscine</strong>. Ce mois intensif a forgé mon autonomie et mon esprit collaboratif.</p>
                    <p>Admis en <strong>décembre 2018</strong>, j'ai suivi un cursus basé sur le peer-learning, développant des compétences en algorithmique et architecture système.</p>
                    <p>Après 5 ans de projets et de hackathons, j'ai obtenu mon <strong>diplôme de fin de cursus</strong> fin 2023.</p>
                </section>
            </div>
        </div>
    </div>

    <div id="pdf" class="content-card" style="display:none;">
        <div class="pdf-tabs">
            <button class="tab-btn active" onclick="showPDF('a')">Certification LvL22</button>
            <button class="tab-btn" onclick="showPDF('b')">Certification RNCP</button>
            <button class="tab-btn" onclick="showPDF('c')">Cursus</button>
        </div>

        <div id="pdf-a" class="pdf-viewer">
            <canvas id="pdf-canvas"></canvas>
            <div class="pdf-toolbar">
                <button onclick="prevPage('a')" class="ctrl-btn">◀️</button>
                <span class="page-info"><span id="page-num-a"></span> / <span id="page-count-a"></span></span>
                <button onclick="nextPage('a')" class="ctrl-btn">▶️</button>
                <a href="{{ '/assets/data/42/pdf/certLvL22.pdf' | relative_url }}" download class="btn-download">💾 Download</a>
            </div>
        </div>

        <div id="pdf-b" class="pdf-viewer" style="display:none;">
            <canvas id="pdf-canvas1"></canvas>
            <div class="pdf-toolbar">
                <button onclick="prevPage('b')" class="ctrl-btn">◀️</button>
                <span class="page-info"><span id="page-num-b"></span> / <span id="page-count-b"></span></span>
                <button onclick="nextPage('b')" class="ctrl-btn">▶️</button>
                <a href="{{ '/assets/data/42/pdf/certRNCP.pdf' | relative_url }}" download class="btn-download">💾 Download</a>
            </div>
        </div>

        <div id="pdf-c" class="pdf-viewer" style="display:none;">
            <canvas id="pdf-canvas2"></canvas>
            <div class="pdf-toolbar">
                <button onclick="prevPage('c')" class="ctrl-btn">◀️</button>
                <span class="page-info"><span id="page-num-c"></span> / <span id="page-count-c"></span></span>
                <button onclick="nextPage('c')" class="ctrl-btn">▶️</button>
                <a href="{{ '/assets/data/42/pdf/cursus.pdf' | relative_url }}" download class="btn-download">💾 Download</a>
            </div>
        </div>
    </div>
</div>

<style>
/* --- DESIGN SYSTEM 42 --- */
:root {
    --42-cyan: #00babc;
    --42-dark: rgba(15, 15, 15, 0.9);
    --42-white: #ffffff;
}

body {
    background: url('{{ "/assets/data/42/img/bkgrnd.jpg" | relative_url }}') no-repeat center center fixed;
    background-size: cover;
    margin: 0;
    color: var(--42-white);
}

.page-42-wrapper {
    max-width: 1100px;
    margin: 40px auto;
    padding: 20px;
    font-family: 'Monaco', 'Consolas', monospace;
}

/* Forcer la navigation en blanc */
.main-nav a, #accordion-toggle {
    color: var(--42-white) !important;
    text-decoration: none;
}

#accordion-toggle a {
    color: var(--42-black) !important;

}

/* --- HEADER --- */
.section-header h1 {
    font-size: 3rem;
    text-align: center;
    text-transform: uppercase;
    letter-spacing: 5px;
    margin-bottom: 30px;
    color: var(--42-white) !important;
    text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.8), 0 0 20px rgba(0, 186, 188, 0.4);
}

.section-header .glitch {
    color: var(--42-cyan);
}

/* --- BOUTONS PRINCIPAUX --- */
.main-nav-buttons {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin-bottom: 40px;
}

.btn-42 {
    background: rgba(0, 0, 0, 0.6);
    border: 2px solid var(--42-cyan);
    color: var(--42-cyan);
    padding: 12px 25px;
    font-weight: bold;
    cursor: pointer;
    transition: 0.3s;
    border-radius: 4px;
    text-transform: uppercase;
}

.btn-42:hover {
    background: var(--42-cyan);
    color: #000;
    box-shadow: 0 0 20px var(--42-cyan);
}

/* --- CARDS & LAYOUT --- */
.content-card {
    background: var(--42-dark);
    border-radius: 15px;
    padding: 30px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(0, 186, 188, 0.3);
    box-shadow: 0 20px 50px rgba(0,0,0,0.7);
}

.story-layout {
    display: grid;
    grid-template-columns: 300px 1fr;
    gap: 30px;
    align-items: start;
}

.profile-img {
    width: 100%;
    border-radius: 10px;
    border: 2px solid var(--42-cyan);
}

/* --- PDF TABS --- */
.pdf-tabs {
    display: flex;
    gap: 10px;
    margin-bottom: 25px;
    justify-content: center;
    flex-wrap: wrap;
}

.tab-btn {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid var(--42-cyan);
    color: var(--42-white);
    padding: 8px 18px;
    cursor: pointer;
    border-radius: 4px;
    transition: 0.3s;
}

.tab-btn.active {
    background: var(--42-cyan);
    color: #000;
    font-weight: bold;
}

/* --- PDF VIEWER --- */
.pdf-viewer { text-align: center; }

canvas {
    max-width: 100%;
    height: auto !important;
    background: white;
    border-radius: 4px;
}

.pdf-toolbar {
    margin-top: 20px;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 20px;
    background: rgba(0,0,0,0.7);
    padding: 12px;
    border-radius: 50px;
}

.ctrl-btn {
    background: none;
    border: none;
    font-size: 1.2rem;
    cursor: pointer;
    color: white;
}

.btn-download {
    background: #28a745;
    color: white !important;
    padding: 6px 15px;
    border-radius: 4px;
    text-decoration: none;
    font-size: 0.9rem;
}

/* Responsive */
@media (max-width: 768px) {
    .story-layout { grid-template-columns: 1fr; text-align: center; }
    .section-header h1 { font-size: 2rem; }
}
</style>

<script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js"></script>
<script>
    const pdfFiles = {
        a: '{{ "/assets/data/42/pdf/certLvL22.pdf" | relative_url }}',
        b: '{{ "/assets/data/42/pdf/certRNCP.pdf" | relative_url }}',
        c: '{{ "/assets/data/42/pdf/cursus.pdf" | relative_url }}'
    };

    const pdfStates = {
        a: { pdfDoc: null, pageNum: 1, pageCount: 0, canvasId: 'pdf-canvas' },
        b: { pdfDoc: null, pageNum: 1, pageCount: 0, canvasId: 'pdf-canvas1' },
        c: { pdfDoc: null, pageNum: 1, pageCount: 0, canvasId: 'pdf-canvas2' }
    };

    function renderPage(key) {
        const state = pdfStates[key];
        state.pdfDoc.getPage(state.pageNum).then(page => {
            const viewport = page.getViewport({ scale: 1.5 });
            const canvas = document.getElementById(state.canvasId);
            const ctx = canvas.getContext('2d');
            canvas.height = viewport.height;
            canvas.width = viewport.width;
            page.render({ canvasContext: ctx, viewport: viewport });
            document.getElementById(`page-num-${key}`).textContent = state.pageNum;
            document.getElementById(`page-count-${key}`).textContent = state.pageCount;
        });
    }

    function showPDF(key) {
        // Switch section visibility
        ['a', 'b', 'c'].forEach(k => {
            document.getElementById(`pdf-${k}`).style.display = (k === key) ? 'block' : 'none';
        });

        // Switch tab active class
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.classList.remove('active');
            if (btn.getAttribute('onclick').includes(key)) btn.classList.add('active');
        });

        if (!pdfStates[key].pdfDoc) {
            pdfjsLib.getDocument(pdfFiles[key]).promise.then(pdf => {
                pdfStates[key].pdfDoc = pdf;
                pdfStates[key].pageCount = pdf.numPages;
                renderPage(key);
            });
        }
    }

    window.onload = () => { showPDF('a'); };

    function prevPage(key) { if (pdfStates[key].pageNum > 1) { pdfStates[key].pageNum--; renderPage(key); } }
    function nextPage(key) { if (pdfStates[key].pageNum < pdfStates[key].pageCount) { pdfStates[key].pageNum++; renderPage(key); } }

    document.getElementById('btn-parcours').onclick = () => {
        document.getElementById('comment').style.display = 'block';
        document.getElementById('pdf').style.display = 'none';
    };
    document.getElementById('btn-documents').onclick = () => {
        document.getElementById('pdf').style.display = 'block';
        document.getElementById('comment').style.display = 'none';
    };
</script>