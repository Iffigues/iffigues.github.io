version: '3.8'

services:
  api:
    build: .
    container_name: github-search-app
    restart: always
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - GITHUB_TOKEN=${GITHUB_TOKEN}
  